# Contributing

1. Fork and clone the repository.
2. Install dependencies with `npm ci`.
3. Make the change and add or update tests.
4. Run `npm run check`.
5. Open a pull request describing the behavior change.

Please do not include personal Logseq graph data, credentials, or generated `node_modules` files.
